import { Roboto, Work_Sans } from 'next/font/google';

export const roboto = Roboto({ subsets: ['cyrillic'], display: 'swap', weight: ['400', '500', '700'] });
export const work_sans = Work_Sans({ subsets: ['latin'], display: 'swap', weight: '700' })